# GH200/aarch64 安装顺序敏感，务必按 environment.md 的说明操作：
# 1) torch 必须先从 cu129 index 装（PyPI aarch64 wheel 是 CPU-only；cu130 与 sgl-kernel 冲突）：
#    pip install torch==2.9.1 torchaudio==2.9.1 torchvision==0.24.1 \
#        --index-url https://download.pytorch.org/whl/cu129
# 2) 然后再装本文件：pip install -r requirements.txt
verl[sglang,math]==0.8.0
sglang[openai,srt]==0.5.8   # verl 0.8.0 sglang extra 的官方 pin，显式写出
sgl-kernel==0.3.21          # aarch64 wheel 为 CUDA 12 构建 → torch 用 cu129
torch==2.9.1
cachetools                  # verl 0.8.0 漏声明的运行时依赖（llm_server.py 需要）
# 3) flash-attn：verl 0.8.0 训练路径硬依赖（bert_padding unpad）。aarch64 无 PyPI wheel，
#    用上游 GitHub release 的官方 aarch64 wheel（cu13 变体，链接系统 /usr/local/cuda 的
#    libcudart.so.13，与 cu129 torch 共存已验证）：
#    pip install https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3.post1/flash_attn-2.8.3+cu13torch2.9cxx11abiTRUE-cp312-cp312-linux_aarch64.whl
matplotlib==3.11.0 # M1: probe 曲线图
pytest==9.1.1 # M2: 单元测试
peft==0.15.2 # M3: LoRA SFT（0.19 与 sglang 栈的 torchao 0.9 冲突，见 plans/M3.md 偏差表）
accelerate==1.14.0 # M3: HF Trainer 依赖
