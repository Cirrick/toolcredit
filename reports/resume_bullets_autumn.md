# 秋招简历精简版（2026-09-06）

适用方向：大模型算法 / 后训练 / Agent RL。基于截至 M9 的代码及 canonical 产物核对（2026-09-07 更新，新增第五条因果对照）；不包含 M10 草案成果。

**ToolCredit｜多轮工具调用大模型强化学习与信用分配**

- **训练管线**：基于 veRL、SGLang 与 Qwen3-1.7B，在单卡 GH200 上搭建数学推理与 Python 工具调用的多轮 RL 管线，串联 SFT 冷启动与 GRPO 训练；200 步 GRPO 将 MATH500 greedy pass@1 从 SFT 起点的 **59.4% 提升至 76.6%（+17.2 个百分点）**。
- **数据与训练一致性**：利用 Qwen3-8B 蒸馏与拒绝采样构建 **6,056 条工具推理轨迹**，包含 797 条报错恢复样本；通过真实 AgentLoop 重放对齐 SFT 与 rollout 的 token 序列，实现工具返回 token 的 loss masking，并完成 LoRA SFT 冷启动。
- **信用分配改进**：实现轮级优势修正，诊断非守恒修正项引入隐式调用激励的问题；设计 token 加权零和再分配，结合零方差门控及重复调用约束，使 760 题 greedy 评测中用满四次调用预算的轨迹占比由初版 **92.9% 降至 0.8%**，截断率由 **100% 降至 8.7%**；相对标准 GRPO 的准确率差异未达统计显著。
- **评测与实验诊断**：构建覆盖 MATH500、AIME24/25、GSM8K 的 **760 题固定评测集**，完成五个模型阶段共 **19,000 条评测轨迹**；采用逐题配对分析与 **10,000 次分层 bootstrap**，结合奖励塑形、loss-mask 消融区分准确率变化与工具行为偏移。
- **因果对照（M9）**：为区分"学会用工具"与"RL 提升推理"，在同起点、同数据、同配方、同 200 步下训练**无工具对照臂**（唯一变量为 prompt 中的工具 schema，resolved-config diff 精确等于 5 条预注册路径，零框架改动）；在 **15,200 条**新增评测轨迹上按预注册矩阵判读为**无差异**（Δ = −0.92pt，95% CI [−3.55, +1.71]），并将 SFT→GRPO 的 **+12.76pt** 精确分解为 **6.05pt（不再被工具拖累）+ 4.74pt（自身推理提升，CI [+2.11, +7.50]）+ 1.97pt（工具边际价值，CI 跨 0）**，据此给出"该规模下工具净收益不显著、收益集中在 MATH L4–5"的结论与后续方向。

## 数字来源与面试口径

- 第一条：`eval/runs/m7_unified_eval_20260824_201032/metrics/pass_at_k.json`，SFT MATH500 为 297/500，E3 为 383/500；都是有工具协议下 greedy 评测。不能将 +17.2pt 归因于工具本身的独立贡献，NoTool RL 对照尚未完成。
- 第二条：`sft/data/gen/gen_stats.json`、`sft/trace_tokenizer.py`、`sft/train_sft.py`。6,056 为保留轨迹数，不能写成独立题目数（覆盖 3,716 题）。
- 第三条：`rl/custom/turn_advantage_v2.py`、`plans/M8.md` §3、`eval/runs/m8_e5v2_eval_20260905_134737/metrics/m8_headline.json`。4-call 为初版 706/760、新版 6/760；截断为初版 760/760、新版 66/760，包含轮数预算等停止条件。新版相对标准 GRPO greedy 准确率 74.1% vs 73.3%，配对差 +0.79pt，95% CI [−1.84,+3.42]pt。v2 同时修改了守恒、门控与部分步骤打分条件，不能宣称单独隔离并证明了守恒性的因果贡献，也不能把未检出增益写成证明等效。
- 第四条：M7 的四个模型阶段各 760 × (1 greedy + 4 sampled) = 15,200 条，M8 新增一个阶段 3,800 条，总计 19,000；M8 复用已有 M7 产物，未重复计数。单训练 seed 下按题 bootstrap 不衡量跨 seed 训练方差。E6 的实际结果是 80 步内未见灾难性退化。

本次仅核对文档、实现及既有统计，不重跑训练或评测。旧版 `resume_bullets.md` 保留供参考，投递优先使用本版。
- 第五条：`eval/runs/m9_notool_eval_20260907_175726/metrics/m9_headline.json`、`rl/runs/e3notool_grpo_20260906_220907/analysis/formal_completion_gate.json`、`plans/M9.md` §8/§12。分解的四个 pass@1 在同 760 题上逐项相加严格闭合；其中"起点被工具拖累"一环无预注册 CI，只报点估计。判读为情形 B（未检出差异），不可写成"证明工具无用"；CI 上界 +1.71pt 仍允许小幅正效应。
