# M10 — E7：DAPO 式零方差 group 动态过滤与补采样

状态：**草案 v2 已由用户批准（2026-09-09）。自此计划原文不再追改；进入 §8 步骤 0（exact boundary 专项审批）。未创建任何 E7 代码、config、launcher、测试或 run。**

计划日期：2026-09-06（v1）；2026-09-09 按用户逐条决定修订为 v2（修订项见 §0）。

上游依据：`PLAN.md` §9.3（E7 定义，可选项）；`plans/M5.md` §3（E7 实验定义与 `score` 口径）；
`plans/M5_E7_IMPLEMENTATION_REVIEW.md`（pin veRL 0.8.0 的 exact boundary，2026-08-22 审查）；
`reports/docs/grpo_failure_diagnostic_session_summary.md` §8/§9.4（E3 零方差统计）；
`reports/qa_log.md` Q12 发现 3（E5-v1 零方差组 25%→48%）、Q14、Q17（pod OOM 根因）、Q22（DAPO vs Dr.GRPO，用户决定先做 M10）；
`HANDOFF.md` 2026-09-09 两条（M10 适用性审查；offload 关闭验证 `rl/runs/e3_offload_check_20260909_150849`）。

> 约定 #7：本节以下"计划原文"在批准后不得追改；执行偏差记入 §12，验收结果记入 §13。
> 禁止事项 #3：E7 改写 trainer 内部数据流，**实现前必须按 §6 再次展示 exact diff boundary 并获专项批准**。

---

## 0. v1 → v2 修订记录（批准前）

| # | 修订 | 用户决定（2026-09-09） |
|---|---|---|
| 1 | 关闭 FSDP CPU offload（三条 diff 进允许集合），每 50 个有效 step 计划性分段恢复，跑的过程中同步定位匿名内存泄漏进程 | 选 A |
| 2 | 补采样倍率由 1.8× 改为期望 2.08–2.70 批（整批采、surplus 丢弃），单步时间 1.4–1.6×，GPU/磁盘预算重算；保留整批补采样，不做按需缩批 | 选 A |
| 3 | 冻结等算力比较规则：轨迹数为主轴、[0, 102,400] 共同区间、线性插值；新增等算力 checkpoint 与评测角色 `e7_equal_compute`；协议 v6，7,600 条 | 接受 |
| 4 | §9 判读矩阵改为四格（Δ_step × Δ_compute），难度漂移降为描述性报告 | 接受 |
| 5 | §1 机制表述补正：零优势只令 PG 项为零，序列仍进入 KL 项与 token-mean 分母；只改表述与监控，不加实验 | 可以 |
| 6 | 对照臂保持 E3 TIR，M9 NoTool 零方差统计只作旁证；协议 v6；修订批准后进入 §8 步骤 0 | 确认 |

---

## 1. 目标与范围

E3 全部 200 step 的离线统计：按真正进入 GRPO advantage 的标量 `score` 计，零方差 group 均值 44.07%，
最后 25 step 约 52.75%，历史最低单步 informative 39.06%。这些 group 的 advantage 全为 0，消耗 rollout
算力却不贡献 policy gradient；且比例随训练上升（题目被学会后变全对）。

**机制补正（v2）**：零优势只使 GRPO 的 policy-gradient 项为零。这些序列仍进入 (i) actor 的 KL loss 项
（`kl_loss_coef=0.001`，low_var_kl），(ii) `token-mean` 聚合的分母。过滤把它们从两处一起拿掉：去掉了对梯度
量级的稀释（等价于一个随零方差比例上升而悄悄下降的学习率），同时改变 PG 与 KL 的相对权重和训练分布。
E7 与 E3 的差异因此不只是"多了有梯度的组"，报告时须按此表述；KL 曲线对照见 §4。

M10 只回答一个问题：**在 E3 配方不变的前提下，丢弃 `std(score)=0` 的 group 并用同一 actor snapshot 补采样到
64 个 informative group，是否提高每个有效 update 的学习效率、每单位 rollout 算力的学习效率，以及终点准确率？**

范围内：trainer adapter 实现（按 §6 边界）、CPU fixtures、5-step smoke、200 有效 step 正式 run（4 段）、协议 bump 到
v6 新增两个 role、与 E3 的配对分析、文档；随 run 定位匿名内存泄漏进程（只诊断，不修）。

范围外：改 reward、mask、advantage、优化器任何项；与 E5-v2 组合（另立计划）；额外 seed；改 E3 canonical；
修泄漏；按需缩小补采样批。对照臂为 E3 TIR；M9 NoTool 的零方差统计（44.6%→56.3%）只作旁证。

---

## 2. 预期与为什么值得跑

结果大体可预期，价值在于把已有诊断数字变成可引用的曲线，并给出 compute-normalized 的诚实比较。
DAPO 原文的学习曲线横轴是训练步数，只用一句话说明总时间没有显著增加，没有给出按算力归一化的曲线；
本计划补这个空档。

| 视角 | 预期 | 理由 |
|---|---|---|
| 按有效 step 的曲线 | E7 早期更快（每步 64 个 informative 组 vs E3 约 36 个） | 有效梯度样本约 1.8× |
| 按累计 rollout 轨迹数的曲线 | E7 与 E3 接近或略差 | 补采样的 rollout 不是免费的 |
| 等算力 checkpoint vs E3 step-200 | 持平（CI 跨 0） | 同样 102,400 条轨迹 |
| 终点（200 有效 step） | 持平或略高（+0–3pt） | E3 已收敛区间，训练量差异不大 |
| 数据难度漂移 | E7 的 informative 集合向 mixed 题集中，后期补采样批次增多 | 全对题被自动跳过；只描述，不作判据 |

**必须同时报告两种 x 轴与等算力配对**，否则会把"多花 2–2.7 倍 rollout"写成"算法更快"。

**补采样成本（v2 重算）**：每个有效 update 整批采 64 题×8，surplus 丢弃、不跨 snapshot 回收。以 E3 informative
比例 .5593（早期）/.4725（后期）作独立同分布二项近似，最多 4 批时期望批数约 2.08 / 2.70（预算推算，非 E7 实测）。
E3 实测单步 161 s 中生成约 55 s、判分约 3 s；E7 只有生成与判分随批数放大，old-log-prob/ref/update 只对选中的 64 组做，
故单步约 224–260 s（1.4–1.6×）。

---

## 3. 冻结语义

### 3.1 过滤规则

- 单位是 UID（同一 prompt 的 8 条 rollout）；
- 零方差判定用实际标量 `score`（answer + 0.1 format），数值容差 `max − min < 1e-8` 且 `std < 1e-8` 双重检查；
  **不按 `acc` 全对/全错判**（约 7.38% 的 group `acc` 一致但 `score` 有 format 方差，仍有梯度）；
- 每个 generation chunk 取 64 个新 prompt × 8 rollout，与 E3 单步完全相同（整批采，不按需缩批）；
- 一个有效 update 最多 4 个 chunk（首批 + 最多补 3 批）；按采样顺序取前 64 个 informative group 组成 actor batch；
- 多出的 informative group 作为 surplus 存证据，**不跨 actor snapshot 回收**；零方差 group 存证据，不训练；
- 4 批仍不足 64 个 informative group：在 actor update 前显式失败并保存当步全部候选，不做 underfilled update；
- 同一有效 update 的全部候选由同一冻结 actor 生成；补采样期间不调 `_update_actor()`、不同步权重；
- 每个有效 update 恰好一次 `on_batch_end(selected_batch)`、一次 actor update。

### 3.2 与 E3 严格不变的项

起点、数据、reward 函数与 `score` 口径、loss mask、old/ref log prob、GRPO 优势（norm-by-std）、optimizer、
KL/clip、validation cadence（有效 step 0/25/…/200）、`max_actor_ckpt_to_keep=3`、seed。

resolved-config diff gate：E3 → E7 只允许 `algorithm.filter_groups.{enable,metric,max_num_gen_batches}`、
trainer class 标记、`project_name`、`experiment_name`，以及**三条纯基础设施差异**
`actor_rollout_ref.actor.fsdp_config.{param_offload,optimizer_offload}=false`、
`actor_rollout_ref.ref.fsdp_config.param_offload=false` 不同。后三条已由
`rl/runs/e3_offload_check_20260909_150849` 验证：formal 形状 5 步 prompt 与 E3 逐步相同，score/loss/KL 在 E3 步间波动内，
GPU max_allocated 43.5 GB 相同、无 OOM；报告中标注"不影响数值，速度可比性受限（gen 阶段 5 步样本偏慢 10–30%，未定论）"。

### 3.3 Ledger（每个有效 update 一行，随 checkpoint 序列化）

`effective_step`、`generation_batches_used`、`prompts_sampled`、`trajectories_sampled`、`rollout_tokens`、
`informative_groups`、`zero_std_groups`、`surplus_groups`、`all_correct/all_wrong/mixed`（acc 口径，仅描述）、
`generation_wall_time`、`filter_wall_time`、累计值（含 `cumulative_trajectories`）、`equal_compute_checkpoint`（布尔）。
checkpoint 与 ledger 原子写；恢复时 ledger step 不符即失败。

### 3.4 等算力 checkpoint（v2 新增）

`cumulative_trajectories` 首次 ≥ 102,400（E3 全部 200 步的轨迹总数 = 200×64×8）时，该有效 step 结束后额外保存一次
checkpoint 到 `checkpoints/equal_compute_step_<N>/`，不参与 `max_actor_ckpt_to_keep` 轮换，ledger 标记 `equal_compute_checkpoint=true`。
预计落在有效 step 75–95。恰好落在 25 的倍数时与常规 checkpoint 内容相同，仍单独保存一份以保证路径固定。

### 3.5 分段恢复（v2 新增）

200 个有效 step 分 4 段（1–50、51–100、101–150、151–200），每段结束后由 launcher 正常退出并用既有 `--resume` 机制续跑；
恢复时跳过起始验证、resolved config 必须逐字相同、ledger step 必须等于 checkpoint step。分段是基础设施措施，
不改变任何数值语义（optimizer/RNG/dataloader/ledger 均从 checkpoint 恢复）。若第一段实测匿名内存斜率明显低于
0.5 GB/有效 step，后续段长可放宽到 100，记入 §12。

---

## 4. 训练侧指标与门禁

- fixed-100 greedy 曲线 × 两种 x 轴（有效 step；累计 rollout 轨迹数），E3 同图；
- 每步 `generation_batches_used` 分布；若连续 10 个有效 step 用满 4 批，**暂停并报告**（数据池 informative 题接近耗尽的信号）；
- E3 稳定性面板（KL、entropy、length、截断、invalid）与 E3 对照，**`actor/kl_loss` 单列一图**（§1 机制补正的监控），任何病态即暂停；
- underfilled 失败即停，不放宽上限；
- 容器内存：memwatch 60 s 一次，记录 cgroup anon/shmem/file 与按进程的 `Pss_Anon` 增量（修复 `step` 字段读取），
  anon 超过 110 GiB 即在下一个 checkpoint 处提前分段；run 结束给出增长进程排名（诊断产物，不修）。

---

## 5. 评测

协议 bump 到 **v6**：新增两个 role `e7_dynamic_filter_step_200` 与 `e7_equal_compute`（§3.4 checkpoint）及其物化、evaluator 绑定；
panel/generation/tool/verifier/taxonomy/pairing/bootstrap 语义不变并用语义校验器证明；只生成 2×3,800 = 7,600 条，其余角色复用 canonical。

配对：E3 → E7-step-200（等 step，greedy）；E3 → E7-equal-compute（等算力，greedy，**primary 之一**）；sampled exploratory。
taxonomy 只做 coarse proposal。

---

## 6. 实现边界（沿用 2026-08-22 审查，实现前须按当时 pin 源码重新核对并专项批准）

| 文件 | 改动 |
|---|---|
| `rl/custom/dynamic_filter_trainer.py` | `DynamicFilterRayPPOTrainer(RayPPOTrainer)`；纯函数 `select_informative_groups`；`DynamicSamplingLedger`；override `fit()`：复制 pin `ray_trainer.py:1362–1770` 约 409 行，只在 `:1422–1525`（dataloader/generation/reward）与 old-log-prob 之间插入有界补采样循环，并在 `_save_checkpoint` 之后按 §3.4 条件追加一次等算力保存，其余逐行同源；`_save_checkpoint/_load_checkpoint` 小 override 写/读 ledger |
| `rl/launch/e7_dynamic_filtering.py` | `DynamicFilteringTaskRunner(TaskRunner)`：复制 `main_ppo.py:223–315` 约 93 行，只把 `:299` 的 trainer 类换掉；经 `run_ppo(config, task_runner_class=...)` 扩展点启动；启动断言并打印实际 trainer 类、`metric=score`、64/8/4、offload 三标志为 false；支持 `--stop-at-step` 分段与 `--resume` |
| `rl/configs/e7_dynamic_filtering.yaml` | 从 E3 复制，只加 `algorithm.filter_groups.{enable: true, metric: score, max_num_gen_batches: 4}` 与三条 offload=false |
| `rl/validate_e7_run.py` | completion gate：200 有效 step、4 段 recovery ledger、ledger 与训练 JSONL 一致、候选去重、surplus/zero-std 不进入训练、等算力 checkpoint 存在且 step 与 ledger 一致 |
| `scripts/m10/*.sh` | tmux 入口（分段循环）、watcher、memwatch v2 |
| `rl/custom/CHANGES.md` | 精确记录覆盖的方法、行号、pin SHA、行为差异、回退路径 |

**必须先做的核对**：`ray_trainer.py` 与 `main_ppo.py` 当前 SHA256 是否仍等于审查记录的
`de58d295…6d66` / `e3fe6e73…1392`（2026-09-09 复核仍相同）；不等则重新审查边界，不凭旧行号写代码。

不改 veRL site-packages、不 fork、不让 E3 launcher 自动选 E7 trainer。用 upstream 文件 SHA 与差分测试锁定复制段，
防止无声漂移。

---

## 7. 测试与 fixtures

- **Fixture N — 过滤纯函数**：构造 64×8 的 score 张量（含全对、全错、`acc` 一致但 format 有方差、mixed），断言 selected/zero-std/surplus 划分、守恒（三者之和等于输入）、`score == rm_scores.sum(-1)`；
- **Fixture O — 多批拼接**：2–4 个 chunk 的 UID/score/tensor 对齐；选中前 64 个 informative 的顺序；balance/meta 只在最终 batch 上执行一次；
- **Fixture P — underfilled 失败**：4 批不足 64 时在 actor update 前 raise，候选已落盘；
- **Fixture Q — ledger/resume**：checkpoint 写入后 ledger 同目录、step 一致；恢复后重算 step 的候选按 `(effective_step, generation_batch, uid)` 去重；分段 `--stop-at-step` 后 resume 的 ledger 连续；
- **Fixture R — fit() 同源**：复制段与 pin 源码逐行 diff，只允许白名单行不同；
- **Fixture S — 等算力触发**：累计轨迹数序列跨过 102,400 时恰触发一次额外保存，之前/之后不触发；恰在 25 倍数处仍单独保存；
- **Fixture T — AUC 规则**：两条人造曲线在轨迹数轴上线性插值、[0, 102,400] 积分归一化，与手算一致；E3 自身两种 x 轴 AUC 相等；
- config diff gate（含三条 offload）；原全部回归与 freeze 闭包校验通过。

---

## 8. 执行顺序与授权门禁

0. **pin 源码复核 + exact boundary 展示**（无代码）：§6 表 + 拟复制行号 + 白名单 diff 行（含等算力保存插入点）；**用户专项批准后**才进入 1。
1. **实现 + 测试**：§6、§7；全仓回归。
2. **磁盘/GPU preflight**：`remaining_peak_write + 30 GiB` 现场重算（§10）；memwatch v2 就位。
3. **5-step smoke**：验证补采样循环真实发生（ledger `generation_batches_used ≥ 2` 至少一步）、同 snapshot 断言、sleep/wake 无异常、`save_freq=-1`、`--stop-at-step` 与 resume 各走一次、匿名内存斜率读数。
4. **200 有效 step formal，4 段**：tmux + watcher；§4 门禁；每段结束核对 ledger 与 recovery ledger；第一段后按 §3.5 决定段长。
5. **评测 + 配对**（v6，两个 role）。
6. **文档**：02 新增 M10 节、technical report、README、LOG/HANDOFF、resume、qa_log（含泄漏进程诊断结论）；commit + tag `m10`。

硬停点：SHA 不符、任何 fixture 失败、underfilled、连续 10 步用满 4 批、稳定性病态、需改 veRL 内部或计划原文、
超预算 50%、非计划性 pod 中断。

---

## 9. 预注册解读矩阵（v2）

两个主判据：

- **Δ_step**：fixed-100 greedy 曲线按有效 step 的 0–200 归一化 AUC 之差（E7 − E3），验证点 0/25/…/200 直接对齐；
- **Δ_compute**：M7-panel greedy FULL760 上 E7 等算力 checkpoint（§3.4）与 E3 step-200 的配对 Δ（E7 − E3）与 bootstrap CI。

"正"= Δ ≥ +2pt 且 95% CI 不跨 0；"负"= Δ ≤ −2pt 且 CI 不跨 0；其余为"持平"。Δ_step 的 CI 用验证点上的 bootstrap 近似，
若无法构造则只用 2pt 阈值并写明。

| 情形 | Δ_step | Δ_compute | 写法 |
|---|---|---|---|
| A（预期最可能） | 正 | 持平 | "过滤把无梯度组的算力换成了更多 informative 组，每步样本效率提高约 x，但每单位 rollout 算力没有免费午餐" |
| B | 正 | 正 | "过滤本身有净收益：同样 102,400 条轨迹下 E7 更准" |
| C | 持平 | 持平 | "E3 的 44% 零方差组对学习曲线影响有限；未检出差异（CI 跨 0 ≠ 无影响）" |
| D | 任意 | 负 | "补采样引入的难度漂移抵消了样本效率，等算力下反而更差" |

其余组合（如 Δ_step 负而 Δ_compute 持平）按最接近的一格写并注明。

附报（不参与落格）：按累计轨迹数的 AUC（共同区间 [0, 102,400]，验证点在轨迹数轴上线性插值后积分并除以区间长度）；
rollout token 数与 wall-clock 两条次级算力轴；step-200 终点配对 Δ；每有效 step 平均批数、总轨迹数（与 E3 102,400 对照）；
informative 集合的 level / mixed 比例漂移（描述性）。

---

## 10. 算力与存储（v2 重算）

| 项 | 估计 | 依据 |
|---|---|---|
| 实现 + 测试 | 2 天 | 409 行 fit() 复制 + 7 组 fixture + 分段/等算力逻辑，四个计划中实现风险最高 |
| smoke | < 1 h | 单步约 1.4–1.6× |
| formal 200 有效 step | 12.5–14.5 h GPU + 4 次分段重启各 5–8 min + 9 次验证 | E3 161 s/步 × 1.4–1.6 |
| 评测 7,600 条 | 2–4 h | M8/M9 吞吐 |
| 磁盘 | run 约 90 GiB（3 轮换 checkpoint + 1 等算力 checkpoint，各 22 GiB；候选 raw JSONL 约 E3 429 MB × 2.1–2.7） | 实测 |

时间盒：实现+测试 2 天；smoke+formal 1.5 天；评测+文档 1 天。超预算 50% 触发 `PLAN.md` §12.2：
若 fit() 复制无法通过同源 diff 测试，E7 以"exact boundary 已审、实现受阻"入报告，不以 YAML-only 或 mask-only 近似替代。

---

## 11. 与 M9 的关系

M9 已于 2026-09-07 完成（情形 B）。M10 独立于 M9；M9 的 NoTool 零方差统计只在讨论中作旁证，不进入 M10 的任何判据。

---

## 12. 执行偏差与决策记录

（批准后执行时填写；计划原文不改。）

| 日期 | 偏差/决策 | 原因 | 影响 |
|---|---|---|---|
| 2026-09-09 | 步骤 0 完成：SHA 复核相同；exact boundary 见 `plans/M10_STEP0_BOUNDARY.md`。用户授权助手自行决定三项：接受 8 处白名单；**驱动边界改用 E5 同款模块名绑定替换（0 行复制）而非 §6 原文的 93 行 `TaskRunner.run()` 复制**；resume 对上游 epoch-boundary 启发式显式拒绝 | 同机制已在 E5/E5-v2 三条 formal run 验证；复制 93 行无额外收益 | §6 表第 2 行按此执行；§13 第 0 行通过 |

---

## 13. 验收表

| # | 验收项 | 判据 | 结果 |
|---|---|---|---|
| 0 | exact boundary 专项批准 | pin SHA 复核 + 白名单 diff 行展示（含等算力保存点），用户批准 | 通过（2026-09-09；`plans/M10_STEP0_BOUNDARY.md` §8，用户授权助手决定） |
| 1 | 测试 | Fixture N–T + 回归全绿；config diff gate（含 offload 三条）通过 | |
| 2 | smoke | 5/5、补采样真实发生、同 snapshot 断言、stop/resume 一次、无 checkpoint 残留 | |
| 3 | formal | 200 有效 step、4 段 recovery ledger、ledger 完整、等算力 checkpoint、五件套 | |
| 4 | 门禁 | 无 underfilled、无连续 10 步满 4 批、稳定性面板与 kl_loss 对照正常、无非计划中断 | |
| 5 | 评测 | v6 语义验证、7,600/7,600、hash ledger | |
| 6 | 配对分析 | E3→E7 两组配对 + bootstrap；两种 x 轴的 AUC；次级算力轴 | |
| 7 | 判读 | 落入 §9 A/B/C/D 之一并按预注册写法成文 | |
| 8 | 泄漏诊断 | 增长进程排名与斜率写入报告与 qa_log | |
| 9 | 文档 | 02/technical report/README/LOG/HANDOFF/resume/qa_log 更新，tag `m10` | |
