# M0 — 环境打通与 smoke test

状态：**服务器侧验收通过**（2026-07-12，tag `m0`）｜5090 本地侧待用户复跑

## 批准的计划（2026-07-12）

目标（PLAN §4）：GH200 上装 pin 版 veRL，原样跑通官方 multi-turn tool 示例，
再缩成 20 条数据 <10 分钟的 smoke test。时间盒 2 天。

1. 平台验证优先：conda 建 py3.12 环境 `toolcredit`；装 aarch64 CUDA torch；GPU 冒烟。
2. 选定并 pin veRL 版本及 rollout 引擎；安装。
3. 原样跑官方 multiturn 示例（不改代码，tmux 后台）。
4. 缩成 `scripts/run_smoke_test.sh`（20 条、1 梯度步、<10min）。
5. 收尾：environment.md、commit + tag m0。

预判风险：sglang/vllm aarch64 wheel 缺失；flash-attn 无 wheel；NFS IO；无 docker（NGC 不可用）。

## 执行偏差与决策记录

| 事项 | 计划 | 实际 |
|---|---|---|
| 官方示例 | 预期 examples/sglang_multiturn 的 GSM8K 示例 | v0.8.0 已移除该目录，改用官方现行路径 agent-loop 教程（ReAct+代码沙箱+**MATH**+GRPO），更贴合本项目 |
| torch 安装 | 直接 aarch64 CUDA wheel | 两次改道：PyPI aarch64 wheel 是 CPU-only → cu130 → sgl-kernel 是 CUDA12 构建 → 定格 **cu129** |
| flash-attn | "sdpa 先跑通，后补" | flash_attn 是 verl 训练路径硬依赖（bert_padding unpad）；用上游 GitHub 官方 aarch64 wheel（cu13 变体，系统 /usr/local/cuda 提供 libcudart.so.13），零编译 |
| 框架改动 | 无 | 一行补丁修 verl 0.8.0 sglang kernel 检查 bug（**用户批准方案 A**，rl/custom/CHANGES.md） |
| 额外补装 | — | cachetools（verl 0.8.0 漏声明）；conda 26 新环境需 ensurepip |

完整踩坑清单见 environment.md。

## 验收结果

| 验收项 | 标准 | 结果 |
|---|---|---|
| 官方示例原样跑通 | rollout、工具调用、训练 loop 全部工作 | ✅ run #5（logs/run_20260712_215334.log）：step0 val acc **0.76**、平均 3.56 轮工具调用（max 18）；5/5 GRPO 步完成，entropy 0.19–0.24、KL ~1e-4、grad_norm <1、40–52s/步、显存峰值 45GB |
| smoke test <10min | 20 条 → 生成 → 验证 → 1 次梯度更新 | ✅ 约 **2 分钟**（22:00:08 → ~22:02，logs/run_20260712_220008.log），step1 指标正常 |
| environment.md 完成 | 版本表 + 平台注意事项 | ✅ 含 6 个坑的完整记录与复现顺序 |
| 5090 侧复跑 | 本地调试链路验证 | ⬜ 待用户执行（requirements.txt 注释顺序 + scripts/run_smoke_test.sh） |

## 遗留观察项

- 训练收尾阶段 `DataLoader worker killed by signal: Killed` 出现 2 次（两 run 模式一致），
  均自愈完赛。M4 长跑时监控是否恶化；怀疑方向：宿主内存瞬时峰值 / dataloader worker 数。
